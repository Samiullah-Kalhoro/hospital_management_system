
import 'package:flutter/material.dart';

class AdminCredentials extends StatelessWidget {
  const AdminCredentials({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}